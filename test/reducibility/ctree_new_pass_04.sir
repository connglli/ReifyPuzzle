// EXPECT: PASS
// COMPILER_ARGS: --dump-control-tree

// Test 4: Control tree for a simple loop.
fun @main(): i32 {
    let mut %x: i32 = 0;
^entry:
    br ^head;
^head:
    br %x < 10, ^body, ^exit;
^body:
    %x = %x + 1;
    br ^head;
^exit:
    ret %x;
}
