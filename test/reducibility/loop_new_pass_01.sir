// EXPECT: PASS
// COMPILER_ARGS: --dump-loops

// Test 1: Simple loop with a single latch and exit.
fun @main(): i32 {
    let mut %x: i32 = 0;
^entry:
    br ^head;
^head:
    br %x < 10, ^body, ^exit;
^body:
    %x = %x + 1;
    br ^head;
^exit:
    ret %x;
}
