// EXPECT: PASS
// COMPILER_ARGS: --dump-domtree

// Test 6: Loop with multiple exits.
fun @main(): i32 {
    let mut %x: i32 = 0;
^entry:
    br ^head;
^head:
    br %x < 10, ^body, ^exit;
^body:
    %x = %x + 1;
    br %x == 5, ^exit, ^head;
^exit:
    ret %x;
}
