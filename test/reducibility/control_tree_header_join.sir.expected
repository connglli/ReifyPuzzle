control-tree @main:
  block ^entry
  loop 0 header=^head
    block ^head
    if ^head
      then:
        block ^a
        fallthrough ^join
      else:
        block ^b
        fallthrough ^join
    block ^join
    if ^join
      then:
        continue ^head levels=0
      else:
        block ^exit
        return ^exit
