// EXPECT: PASS
// SPEC v0.2.2 §6.10(b)
// Intention: multi-arg call with matching arity is well-typed.
fun @sum4(%a: i32, %b: i32, %c: i32, %d: i32) : i32 {
^entry:
  ret %a + %b + %c + %d;
}

fun @main(%x: i32) : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @sum4(%x, %x, %x, %x);
  ret %r;
}
