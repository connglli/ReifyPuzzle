// EXPECT: PASS
// SPEC v0.2.2 §6.10(a)
// Intention: a call to an intrinsic declared later in source must be rejected.
fun @main(%a: i32) : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @abs(%a);
  ret %r;
}

intrinsic @abs(%x: i32) : i32;
