// EXPECT: PASS
// SPEC v0.2.2 §6.10 (result)
// Intention: call result type matches assignment target.
fun @f(%x: i32) : i32 {
^entry:
  ret %x;
}

fun @main(%a: i32) : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @f(%a);
  ret %r;
}
