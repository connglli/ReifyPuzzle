// EXPECT: PASS
// SPEC v0.2.2 §6.10 (result)
// Intention: i32-returning call may participate in a Cond.
fun @f(%x: i32) : i32 {
^entry:
  ret %x;
}

fun @main(%a: i32) : i32 {
  let mut %r: i32 = 0;
^entry:
  br call @f(%a) > 0, ^pos, ^np;
^pos:
  %r = 1;
  ret %r;
^np:
  %r = 0;
  ret %r;
}
