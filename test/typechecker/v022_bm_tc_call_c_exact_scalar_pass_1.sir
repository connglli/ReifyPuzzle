// EXPECT: PASS
// SPEC v0.2.2 §6.10(c)
// Intention: exact i32 to i32 parameter match across fun call.
fun @f(%x: i32) : i32 {
^entry:
  ret %x;
}

fun @main(%a: i32) : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @f(%a);
  ret %r;
}
