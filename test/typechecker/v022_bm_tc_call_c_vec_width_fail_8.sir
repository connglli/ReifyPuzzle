// EXPECT: FAIL:StaticError
// SPEC v0.2.2 §6.10(c)
// Intention: <4> i32 cannot be passed where <8> i32 is expected.
fun @f(%v: <8> i32) : i32 {
^entry:
  ret %v[0];
}

fun @main(%v: <4> i32) : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @f(%v);
  ret %r;
}
