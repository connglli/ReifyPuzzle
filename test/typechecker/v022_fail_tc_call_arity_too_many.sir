// EXPECT: FAIL
// Intention: SPEC v0.2.2 Section 6.10 - Call arity mismatch (too many).
fun @foo() : i32 { ^entry: ret 0; }
fun @main() : i32 {
  let mut %b: i32 = 0;
^entry:
  %b = call @foo(1);
  ret 0;
}
