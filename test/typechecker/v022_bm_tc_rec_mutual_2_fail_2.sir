// EXPECT: FAIL:StaticError
// SPEC v0.2.2 §9.7
// Intention: 2-cycle mutual recursion @a -> @b -> @a must be rejected.
fun @a(%x: i32) : i32 {
^entry:
  ret call @b(%x);
}

fun @b(%x: i32) : i32 {
^entry:
  ret call @a(%x);
}

fun @main(%x: i32) : i32 {
^entry:
  ret call @a(%x);
}
