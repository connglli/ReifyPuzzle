// EXPECT: FAIL:StaticError
// SPEC v0.2.2 §6.11
// Intention: even a single pre alone (no message) must fail without a post.
decl @f(%x: i32) : i32 {
  pre %x >= 0;
};

fun @main(%a: i32) : i32 {
^entry:
  ret call @f(%a);
}
