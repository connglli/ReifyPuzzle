// EXPECT: FAIL:StaticError
// SPEC v0.2.2 §6.11
// Intention: multiple pres with no post clauses must be rejected.
decl @f(%a: i32, %b: i32) : i32 {
  pre %a > 0, "positive";
  pre %b > 0, "positive";
};

fun @main(%x: i32, %y: i32) : i32 {
^entry:
  ret call @f(%x, %y);
}
