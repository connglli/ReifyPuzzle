// EXPECT: PASS
// SPEC v0.2.2 §6.11 (post)
// Intention: post ret == %x + 1 references params and ret.
decl @f(%x: i32) : i32 {
  post ret == %x + 1, "x+1";
};

fun @main(%a: i32) : i32 {
^entry:
  ret call @f(%a);
}
