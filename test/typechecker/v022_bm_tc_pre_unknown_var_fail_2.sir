// EXPECT: FAIL:StaticError
// SPEC v0.2.2 §6.11 (pre)
// Intention: pre may only reference function parameters; unknown var must fail.
decl @f(%x: i32) : i32 {
  pre %ghost > 0, "unknown";
  post ret == %x, "ok";
};

fun @main(%a: i32) : i32 {
^entry:
  ret call @f(%a);
}
