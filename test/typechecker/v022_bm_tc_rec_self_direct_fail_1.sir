// EXPECT: FAIL:StaticError
// SPEC v0.2.2 §9.7
// Intention: direct self-recursion in a fun body must be statically detected.
fun @rec(%x: i32) : i32 {
^entry:
  ret call @rec(%x);
}

fun @main(%a: i32) : i32 {
^entry:
  ret call @rec(%a);
}
