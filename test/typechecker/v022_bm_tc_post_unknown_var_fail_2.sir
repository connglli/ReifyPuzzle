// EXPECT: FAIL:StaticError
// SPEC v0.2.2 §6.11 (post)
// Intention: post may reference only params ∪ {ret}; unknown var must fail.
decl @f(%x: i32) : i32 {
  post %ghost > 0, "unknown";
};

fun @main(%a: i32) : i32 {
^entry:
  ret call @f(%a);
}
