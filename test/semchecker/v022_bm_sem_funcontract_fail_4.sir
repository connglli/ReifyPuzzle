// EXPECT: FAIL
// SPEC v0.2.2 §3.3
// Intention: fun with multiple post clauses (still a contract on a fun).
fun @f(%x: i32) : i32 {
  post ret > 0;
  post ret <= %x;
^entry:
  ret %x;
}
fun @main() : i32 { ^entry: ret 0; }
