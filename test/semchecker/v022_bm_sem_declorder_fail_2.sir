// EXPECT: PASS
// SPEC v0.2.2 §3.3
// Intention: intrinsic call before its declaration.
fun @main() : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @abs(-1);
  ret %r;
}
intrinsic @abs(%x: i32) : i32;
