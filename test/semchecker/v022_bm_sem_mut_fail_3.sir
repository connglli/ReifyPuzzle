// EXPECT: FAIL
// SPEC v0.2.2 §3.5 §6.6
// Intention: assign to a sym identifier (immutable).
fun @main() : i32 {
  sym %?k: value i32 in [0, 10];
  let mut %r: i32 = 0;
^entry:
  %?k = 5;
  %r = %?k;
  ret %r;
}
