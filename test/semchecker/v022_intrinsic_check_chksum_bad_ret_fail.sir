// EXPECT: FAIL
// [v0.2.2] @check_chksum: every type slot (return + both params) is
// fixed at i32. A non-i32 return type is rejected at sem-check.
intrinsic @check_chksum(%expected: i32, %actual: i32) : i64;
fun @main() : i32 {
^entry:
  ret 0;
}
