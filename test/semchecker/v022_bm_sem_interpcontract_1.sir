// EXPECT: PASS
// SPEC v0.2.2 §11.3
// SKIP: INTERPRETER, COMPILER
// Intention: sem-check should ACCEPT a contract-only decl + caller; interpreter rejects at runtime.
decl @abstract(%x: i32) : i32 {
  pre %x >= 0;
  post ret >= %x;
};
fun @main() : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @abstract(3);
  ret %r;
}
