// EXPECT: FAIL
// SPEC v0.2.2 §3.3
// Intention: TWO decl-contracts for the same name (same-file duplicate contract).
decl @c(%x: i32) : i32 { post ret > 0; };
decl @c(%x: i32) : i32 { post ret < 100; };
fun @main() : i32 { ^entry: ret 0; }
