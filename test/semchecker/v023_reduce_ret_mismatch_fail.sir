// EXPECT: FAIL:StaticError
// SPEC v0.2.3 §12.4 — the return type of a reduction must equal the
// vector's element type.  <4> i32 folds to i32, not i16.
intrinsic @reduce_add(%v: <4> i32) : i16;

fun @main() : i32 {
^entry:
  ret 0;
}
