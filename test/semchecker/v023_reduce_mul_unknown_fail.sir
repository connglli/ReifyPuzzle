// EXPECT: FAIL:StaticError
// SPEC v0.2.3 §12.4 / §13 — @reduce_mul is deliberately NOT provided
// (nonlinear symbolic multiplication chains are solver-hostile); it is an
// unknown intrinsic name.
intrinsic @reduce_mul(%v: <4> i32) : i32;

fun @main() : i32 {
^entry:
  ret 0;
}
