// EXPECT: FAIL
// [v0.2.2] Two intrinsics with the same name AND same parameter types
// are ambiguous (indistinguishable at call sites) and must still be rejected.
intrinsic @min(%a: i32, %b: i32): i32;
intrinsic @min(%a: i32, %b: i32): i32;

fun @main(): i32 {
^entry:
  ret 0;
}
