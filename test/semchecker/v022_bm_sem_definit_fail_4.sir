// EXPECT: FAIL
// SPEC v0.2.2 §3.5
// Intention: undef + initialize via call doesn't help reads BEFORE assignment.
fun @h(%x: i32) : i32 { ^entry: ret %x; }
fun @main() : i32 {
  let mut %y: i32 = undef;
  let mut %r: i32 = 0;
^entry:
  %r = %y * 2;
  %y = call @h(0);
  ret %r;
}
