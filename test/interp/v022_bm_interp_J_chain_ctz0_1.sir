// EXPECT: FAIL
// SPEC v0.2.2 §12 chained intrinsics; inner produces 0 -> ctz UB
intrinsic @ctz(%x: i32) : i32;
intrinsic @max(%a: i32, %b: i32) : i32;
fun @main() : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @ctz(call @max(-5, 0)); // max=0, ctz(0) UB
  ret %r;
}
