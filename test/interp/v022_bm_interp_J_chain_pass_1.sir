// EXPECT: PASS
// SPEC v0.2.2 §12 chained intrinsics
intrinsic @abs(%x: i32) : i32;
intrinsic @min(%a: i32, %b: i32) : i32;
fun @main() : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @abs(call @min(-7, 3)); // min=-7, abs=7
  br %r == 7, ^ok, ^bad;
^ok:
  ret 0;
^bad:
  ret 1;
}
