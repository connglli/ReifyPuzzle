// EXPECT: PASS
// SPEC v0.2.2 §12.2
intrinsic @popcount(%x: i64) : i64;
fun @main() : i32 {
  let mut %r: i64 = 0;
  let %x: i64 = -1; // 64 ones
^entry:
  %r = call @popcount(%x);
  br %r == 64, ^ok, ^bad;
^ok:
  ret 0;
^bad:
  ret 1;
}
