// EXPECT: PASS
// SPEC v0.2.2 §12.1 @min
intrinsic @min(%a: i32, %b: i32) : i32;
fun @main() : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @min(-5, 7);
  br %r == -5, ^ok, ^bad;
^ok:
  ret 0;
^bad:
  ret 1;
}
