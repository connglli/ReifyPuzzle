// EXPECT: FAIL
// Intention: SPEC v0.2.2 Section 12 - @ctz result must be representable.
// ctz(16) (10000 in binary) is 4. 4 DOES NOT fit in i2 (range -2 to 1).
intrinsic @ctz(%x: i32) : i2;

fun @main() : i32 {
  let mut %a: i2 = 0;
^entry:
  %a = call @ctz(16);
  ret 0;
}
