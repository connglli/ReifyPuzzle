// EXPECT: FAIL:UndefinedBehavior
// Vector lane variant of fp_rem_intermediate_overflow_f64: lane 0 hits
// the §2.9 intermediate-overflow rule; lane 1 would be well-defined
// on its own, but per §7.6 rule 21 any single lane's UB makes the
// whole path UB.
fun @main() : f64 {
  let %x: <2> f64 = {1.0e300, 5.0};
  let %y: <2> f64 = {1.0e-300, 2.0};
  let mut %r: <2> f64 = 0.0;
^entry:
  %r = %x % %y;
  ret %r[1];
}
