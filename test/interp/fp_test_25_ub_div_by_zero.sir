// EXPECT: FAIL:UndefinedBehavior
// SKIP: COMPILER
// Intention: Test UB for division of a non-zero float value by zero.
// Purpose: Verify that division by zero (resulting in infinity) is flagged as Undefined Behavior (SPEC Rule 6).
// f32 division of non-zero by 0.0 produces ±inf: UB rule 6
fun @main() : f32 {
  let %a: f32 = 1.0;
  let %b: f32 = 0.0;
  let mut %r: f32 = 0.0;
^entry:
  %r = %a / %b;
  ret %r;
}
