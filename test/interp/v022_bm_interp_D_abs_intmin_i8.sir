// EXPECT: FAIL
// SPEC v0.2.2 §12.1, rule 4/25
// Intention: @abs(INT_MIN) UB at i8 (-128 has no representable absolute value).
intrinsic @abs(%x: i8) : i8;
fun @main() : i32 {
  let mut %r: i8 = 0;
  let %x: i8 = -128;
^entry:
  %r = call @abs(%x);
  ret 0;
}
