// EXPECT: FAIL:UndefinedBehavior
// SKIP: COMPILER
// Intention: Test UB for float operations overflowing to infinity.
// Purpose: Verify that floating-point addition/multiplication overflow to infinity is flagged as Undefined Behavior (SPEC Rule 6).
// f32 addition overflow produces ±inf: UB rule 6
fun @main() : f32 {
  let %a: f32 = 3.4e38;
  let %b: f32 = 3.4e38;
  let mut %r: f32 = 0.0;
^entry:
  %r = %a + %b;
  ret %r;
}
