// EXPECT: FAIL:UndefinedBehavior
// SKIP: COMPILER
// Rule 3: load through pointer to scalar with undef value.
// %x is undef (i32). addr %x gives ptr i32. load reads undef → UB.
fun @main() : i32 {
  let mut %x: i32 = undef;
  let mut %p: ptr i32 = undef;
  let mut %r: i32 = 0;
^entry:
  %p = addr %x;
  %r = load %p;
  ret 0;
}
