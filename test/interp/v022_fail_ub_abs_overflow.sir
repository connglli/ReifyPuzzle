// EXPECT: FAIL:UndefinedBehavior
// Intention: SPEC v0.2.2 Section 12.1 - @abs(INT_MIN) is UB as the result is not representable.
intrinsic @abs(%x: i32) : i32;
fun @main() : i32 {
  let %min: i32 = -2147483648;
  let mut %a: i32 = 0;
^entry:
  %a = call @abs(%min);
  ret %a;
}
