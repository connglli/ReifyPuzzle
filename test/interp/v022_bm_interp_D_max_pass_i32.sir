// EXPECT: PASS
// SPEC v0.2.2 §12.1 @max
intrinsic @max(%a: i32, %b: i32) : i32;
fun @main() : i32 {
  let mut %r: i32 = 0;
^entry:
  %r = call @max(-5, 7);
  br %r == 7, ^ok, ^bad;
^ok:
  ret 0;
^bad:
  ret 1;
}
