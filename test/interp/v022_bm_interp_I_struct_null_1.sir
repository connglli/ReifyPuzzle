// EXPECT: FAIL
// SPEC v0.2.2 §7.5 rule 17 - ptrfield on null in callee
struct @Pt { x: i32; y: i32; }
fun @bad(%p: ptr @Pt) : i32 {
  let mut %fx: ptr i32 = null;
^entry:
  %fx = ptrfield %p, x;
  ret load %fx;
}
fun @main() : i32 {
  let mut %np: ptr @Pt = null;
^entry:
  ret call @bad(%np);
}
