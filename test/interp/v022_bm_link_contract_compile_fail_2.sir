// EXPECT: FAIL
// SPEC v0.2.2 §3.4
// Intention: `ret` is not allowed in `pre` clauses; must error.
decl @bm_ret_in_pre(%x: i32) : i32 {
  pre ret == %x, "bad";
  post ret == %x, "id";
};

fun @main() : i32 {
^entry:
  ret 0;
}
