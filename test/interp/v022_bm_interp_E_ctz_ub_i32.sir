// EXPECT: FAIL:UndefinedBehavior
// SPEC v0.2.2 §12.2 @ctz UB when input is 0
intrinsic @ctz(%x: i32) : i32;
fun @main() : i32 {
  let mut %r: i32 = 0;
  let %a: i32 = 9876;
  let %b: i32 = 9876;
  let mut %input: i32 = 0;
^entry:
  %input = %a - %b;
  %r = call @ctz(%input);
  ret %r;
}
