// EXPECT: FAIL:UndefinedBehavior
// SKIP: COMPILER
// Intention: Test UB for float modulo producing NaN.
// Purpose: Verify that float modulo with a zero divisor (producing NaN) is flagged as Undefined Behavior (SPEC Rule 7).
// f32 x % 0.0 produces NaN: UB rule 7
fun @main() : f32 {
  let %a: f32 = 1.0;
  let %b: f32 = 0.0;
  let mut %r: f32 = 0.0;
^entry:
  %r = %a % %b;
  ret %r;
}
