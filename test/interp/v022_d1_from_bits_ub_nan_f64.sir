// EXPECT: FAIL:UndefinedBehavior
// SPEC v0.2.2 §12.6 @from_bits UB — bit pattern of a quiet NaN
// (0x7FF8000000000000 = 9221120237041090560 as signed i64) is non-finite.
intrinsic @from_bits(%x: i64) : f64;
fun @main() : i32 {
  let mut %r: f64 = 0.0;
^entry:
  %r = call @from_bits(9221120237041090560);
  ret 0;
}
