// EXPECT: FAIL:UndefinedBehavior
// SKIP: INTERPRETER, COMPILER
// SOLVER_ARGS: --main @main --path '^entry'
fun @main(%pa: i8, %vtrue: i32, %vfalse: i32) : i32 {
  let mut %v: i8 = -6;
  let mut %res: i32 = 0;
^entry:
  %res = select 118 * %pa == -104 as i8 - 96 * %v, %vtrue, %vfalse;
  ret %res;
}
