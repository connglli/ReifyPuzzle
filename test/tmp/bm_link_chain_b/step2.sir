// SKIP: ALL — library / fixture file; not a standalone test
decl @bm_chain_step3(%x: i32) : i32;
fun @bm_chain_step2(%x: i32) : i32 {
  let mut %y: i32 = 0;
^entry:
  %y = call @bm_chain_step3(%x);
  ret %y * %x;
}
