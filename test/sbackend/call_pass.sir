// EXPECT: PASS


fun @scale(%v: i32, %k: i32): i32 {
    let mut %r: i32 = 0;
^entry:
    %r = %v * %k;
    ret %r;
}

fun @main(): i32 {
    let mut %x: i32 = 6;
    let mut %r: i32 = 0;
^entry:
    %r = call @scale(%x, 7);
    require %r == 42, "call";
    ret %r;
}
