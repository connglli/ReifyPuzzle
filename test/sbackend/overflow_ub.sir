// EXPECT: FAIL:UndefinedBehavior
// SKIP: WASM — UBs are not guaranteed.


// i32 signed-add overflow is UB and must trap at runtime.
fun @main(): i32 {
    let mut %a: i32 = 2147483647;
    let mut %r: i32 = 0;
^entry:
    %r = %a + 1;
    ret %r;
}
