// EXPECT: FAIL:UndefinedBehavior
// SKIP: C, WASM — structured-backend suite (goto-based c/wasm sweeps skip)

fun @main(): i32 {
    let mut %a: <4> i32 = 0;
    let mut %i: i32 = 6;
    let mut %v: i32 = 0;
^entry:
    %v = %a[%i];
    ret %v;
}
