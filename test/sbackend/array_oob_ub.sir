// EXPECT: FAIL:UndefinedBehavior


// Direct out-of-bounds element access with a dynamic index.
fun @main(): i32 {
    let mut %a: [3] i32 = 0;
    let mut %i: i32 = 5;
    let mut %v: i32 = 0;
^entry:
    %v = %a[%i];
    ret %v;
}
