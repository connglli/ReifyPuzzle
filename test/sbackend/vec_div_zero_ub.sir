// EXPECT: FAIL:UndefinedBehavior
// SKIP: C, WASM — structured-backend suite (goto-based c/wasm sweeps skip)

// Lane-wise UB: one lane divides by zero.
fun @main(): i32 {
    let mut %a: <4> i32 = {8, 8, 8, 8};
    let mut %b: <4> i32 = {2, 4, 0, 8};
    let mut %r: <4> i32 = 0;
^entry:
    %r = %a / %b;
    ret %r[0];
}
