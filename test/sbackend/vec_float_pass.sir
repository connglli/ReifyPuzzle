// EXPECT: PASS


fun @main(): i32 {
    let mut %a: <2> f64 = {1.5, 2.5};
    let mut %b: <2> f64 = {0.5, 0.5};
    let mut %r: <2> f64 = 0.0;
^entry:
    %r = %a + %b;
    require %r[0] == 2.0, "f64 lane add";
    %r = %a / %b;
    require %r[1] == 5.0, "f64 lane div";
    ret 0;
}
