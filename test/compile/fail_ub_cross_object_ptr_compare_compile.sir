// EXPECT: FAIL
// SKIP: INTERPRETER
// SKIP: SOLVER
//
// Intention: Expose missing cross-object pointer comparison check in compiled C.
// Exposes: Missing runtime check / sanitizer failure in compiled C for relational pointer comparison.
// Expected Behavior: Compiled C code traps/aborts on cross-object pointer comparison (<).
// Actual Behavior: Compiled C code runs successfully and exits with 0.

fun @main() : i32 {
  let mut %x: i32 = 1;
  let mut %y: i32 = 2;
  let mut %px: ptr i32 = null;
  let mut %py: ptr i32 = null;
  let mut %res: i1 = 0;
^entry:
  %px = addr %x;
  %py = addr %y;
  %res = %px < %py; // UB: cross-object pointer comparison!
  ret 0;
}
