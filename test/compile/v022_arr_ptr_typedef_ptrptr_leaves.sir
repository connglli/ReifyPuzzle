// EXPECT: PASS
// COMPILER_ARGS: --target c
// Regression: nested pointer leaves (`ptr ptr T`) with different inner
// `T` must not collide on the typedef name either.
fun @main(): i32 {
  let mut %a32: [2] ptr ptr i32 = {undef, undef};
  let mut %a64: [2] ptr ptr i64 = {undef, undef};
  let mut %p32: ptr [2] ptr ptr i32 = undef;
  let mut %p64: ptr [2] ptr ptr i64 = undef;
^entry:
  %p32 = addr %a32;
  %p64 = addr %a64;
  ret 0;
}
