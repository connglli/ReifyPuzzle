// EXPECT: FAIL
// SPEC v0.2.2 §3.3
// Intention: fun cannot have a contract.

fun @bad(%x: i32) : i32 {
  pre %x > 0, "no";
  post ret > 0, "no";
}

fun @main() : i32 {
^entry:
  ret call @bad(1);
}
