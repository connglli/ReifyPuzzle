// EXPECT: FAIL:ParseError
// SPEC v0.2.2 §5.3
// Intention: call missing parens in expression position.
intrinsic @abs(%x: i32) : i32;
fun @main(%a: i32) : i32 {
  let mut %y: i32 = 0;
^entry:
  %y = call @abs + 1;
  ret %y;
}
