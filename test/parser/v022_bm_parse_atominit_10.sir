// EXPECT: FAIL:ParseError
// SPEC v0.2.2 §3.5.2
// Intention: trailing comma in call args.
intrinsic @min(%a: i32, %b: i32) : i32;
fun @main(%a: i32, %b: i32) : i32 {
  let mut %y: i32 = call @min(%a, %b,);
^entry:
  ret %y;
}
