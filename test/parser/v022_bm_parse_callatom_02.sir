// EXPECT: PASS
// SPEC v0.2.2 §5.3
// Intention: call inside `+` chain mixed with other atoms.
intrinsic @abs(%x: i32) : i32;
fun @main(%a: i32) : i32 {
  let mut %y: i32 = 0;
^entry:
  %y = call @abs(%a) + 1 - %a;
  ret %y;
}
