// EXPECT: FAIL:ParseError
// SPEC v0.2.2 §5.3
// Intention: select with only one value (missing false arm).
fun @main(%a: i32) : i32 {
  let mut %y: i32 = 0;
^entry:
  %y = select %a == 0, %a;
  ret %y;
}
