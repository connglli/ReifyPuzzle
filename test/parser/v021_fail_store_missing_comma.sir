// EXPECT: FAIL:ParseError
// `store` requires a comma between ptr expression and value expression.
fun @main() : i32 {
  let mut %x: i32 = 0;
  let mut %p: ptr i32 = null;
^entry:
  %p = addr %x;
  store %p 17;
  ret 0;
}
