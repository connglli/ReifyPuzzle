// EXPECT: FAIL:ParseError
// SPEC v0.2.2 §3.3
// Intention: accidental `{` (would start a body, but decl has no body).
decl @h(%x: i32) : i32 {
^entry:
  ret %x;
}
fun @main() : i32 { ^entry: ret 0; }
