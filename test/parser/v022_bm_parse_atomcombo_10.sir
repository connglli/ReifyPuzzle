// EXPECT: FAIL:ParseError
// SPEC v0.2.2 §5.3
// Intention: ptrindex with `call` as index — Index grammar disallows call.
intrinsic @geti() : i32;
fun @main() : i32 {
  let mut %a: [4] i32 = { 0, 0, 0, 0 };
  let mut %p: ptr i32 = null;
^entry:
  %p = ptrindex addr %a, call @geti();
  ret 0;
}
