// EXPECT: FAIL:ParseError
// SPEC v0.2.2 §5.3
// Intention: select missing the comma between cond and first val.
fun @main(%a: i32) : i32 {
  let mut %y: i32 = 0;
^entry:
  %y = select %a == 0 %a, 0;
  ret %y;
}
