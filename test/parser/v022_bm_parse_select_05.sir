// EXPECT: FAIL:ParseError
// SPEC v0.2.2 §5.3
// Intention: select with `addr` arm (SelectVal forbids non-RValue/Coef atoms).
fun @main(%a: i32) : i32 {
  let mut %x: i32 = 0;
  let mut %p: ptr i32 = null;
^entry:
  %p = select %a == 0, addr %x, null;
  ret 0;
}
