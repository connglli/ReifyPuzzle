// EXPECT: PASS
// Parser must accept pointer arithmetic atoms in expressions.
fun @main() : i32 {
  let mut %arr: [4] i32 = {0, 0, 0, 0};
  let mut %p: ptr i32 = null;
  let mut %q: ptr i32 = null;
  let %one: i32 = 1;
  let mut %diff: i64 = 0;
^entry:
  %p = addr %arr[0];
  %q = %p + %one;
  %p = %q - %one;
  %diff = %q - %p;
  ret 0;
}
