// EXPECT: FAIL:ParseError
// SPEC v0.2.2 §5.3
// Intention: SelectVal forbids `call` (lazy-arm restriction).
intrinsic @get() : i32;
fun @main(%a: i32) : i32 {
  let mut %y: i32 = 0;
^entry:
  %y = select %a == 0, call @get(), 0;
  ret %y;
}
