// EXPECT: PASS
// SPEC v0.2.2 §5.3
// Intention: mask-based select with <N> i1 mask and matching vector arms.
fun @main() : i32 {
  let mut %m: <4> i1 = { 1, 0, 1, 0 };
  let mut %a: <4> i32 = { 1, 2, 3, 4 };
  let mut %b: <4> i32 = { 5, 6, 7, 8 };
  let mut %y: <4> i32 = { 0, 0, 0, 0 };
^entry:
  %y = select %m, %a, %b;
  ret 0;
}
