// EXPECT: FAIL:ParseError
// SPEC v0.2.2 §3.3
// Intention: `pre` clause appears after `post` clause (order violation).
decl @h(%x: i32) : i32 {
  post ret == %x, "id";
  pre %x > 0, "positive";
};
fun @main() : i32 { ^entry: ret 0; }
