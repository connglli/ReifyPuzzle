// EXPECT: PASS
// Intention: SPEC v0.2.2 §3.1 - String with escape `\n` and `\"` in `require`.
fun @main() : i32 {
  let mut %x: i32 = 1;
^entry:
  require %x > 0, "x is positive\nstrictly";
  ret 0;
}
