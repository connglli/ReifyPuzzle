// EXPECT: PASS
// Intention: SPEC v0.2.2 §3.1, §3.3 - `decl` contract form recognized; uses `pre`, `post`, `ret` keywords.
decl @add(%a: i32, %b: i32) : i32 {
  pre %a >= 0;
  post ret == %a + %b;
};
fun @main() : i32 {
^entry:
  ret 0;
}
