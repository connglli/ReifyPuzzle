// EXPECT: FAIL
// Intention: SPEC v0.2.2 §3.3 - `%pre` is a local id; using it where `pre` keyword is expected fails.
decl @f(%x: i32) : i32 {
  %pre %x > 0;
  post ret == %x;
};
fun @main() : i32 {
^entry:
  ret 0;
}
