// EXPECT: FAIL
// Intention: SPEC v0.2.2 §3.1 - `call` is a bare keyword; using `@call` in `call` position (i.e. `@call(...)` without `call` prefix) should fail at parse.
fun @main() : i32 {
  let mut %x: i32 = 0;
^entry:
  %x = @call();
  ret %x;
}
