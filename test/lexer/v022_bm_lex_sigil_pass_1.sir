// EXPECT: PASS
// Intention: SPEC v0.2.2 §1 - All sigils used correctly with v0.2.2 keywords.
decl @f(%x: i32) : i32 { post ret == %x; };
fun @main() : i32 {
  let mut %x: i32 = 0;
^entry:
  %x = call @f(%x);
  ret %x;
}
