// EXPECT: PASS
// Intention: SPEC v0.2.2 §3.1 - `decl_x` is a single identifier; usable as struct field.
struct @S { decl_x: i32; }
fun @main() : i32 {
  let mut %s: @S = { 42 };
^entry:
  ret %s.decl_x;
}
