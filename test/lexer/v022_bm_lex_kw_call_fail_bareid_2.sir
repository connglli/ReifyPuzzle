// EXPECT: FAIL
// Intention: SPEC v0.2.2 §3.1 - `call` is reserved; struct field access `.call` should not parse.
struct @S { x: i32; }
fun @main() : i32 {
  let mut %s: @S = { 0 };
^entry:
  %s.call = 1;
  ret 0;
}
