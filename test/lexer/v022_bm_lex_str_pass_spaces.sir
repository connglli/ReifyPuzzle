// EXPECT: PASS
// Intention: SPEC v0.2.2 §3.1 - String with spaces in `pre` rationale.
decl @f(%x: i32) : i32 {
  pre %x > 0, "x must be strictly positive";
  post ret == %x;
};
fun @main() : i32 {
^entry:
  ret 0;
}
