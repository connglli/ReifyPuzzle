// EXPECT: PASS
// Intention: SPEC v0.2.2 §3.3 - `pre` with rationale message (StringLit).
decl @f(%x: i32) : i32 {
  pre %x >= 0, "x must be non-negative";
  post ret == %x;
};
fun @main() : i32 {
^entry:
  ret 0;
}
